{
    "name": "YouCRUD",
    "version": "1.0.0",
    "description": "My pioneering Nuxt.js project",
    "author": "Pythux",
    "private": true,
    "scripts": {
        "dev": "nuxt -p 8080",
        "build": "nuxt build",
        "start": "nuxt start",
        "generate": "nuxt generate",
        "lint": "eslint --ext .js,.vue --ignore-path ~/.gitignore_global .",
        "lint-fix": "eslint --fix --ext .js,.vue --ignore-path ~/.gitignore_global ."
    },
    "dependencies": {
        "nuxt": "^2.0.0"
    },
    "devDependencies": {
        "@nuxtjs/vuetify": "^1.0.0",
        "@nuxtjs/eslint-config": "^1.0.1",
        "@nuxtjs/eslint-module": "^1.0.0",
        "babel-eslint": "^10.0.1",
        "eslint": "^6.1.0",
        "eslint-plugin-nuxt": ">=0.4.2"
    }
}